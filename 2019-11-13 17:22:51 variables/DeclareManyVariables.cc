#include <iostream>

using namespace std;

int main() {
    // Declare more than one variable of the same type using comma.
    int x = 5, y = 6, z = 50;
    
    // Add the variables
    cout << x + y + z;
    return 0;
}
